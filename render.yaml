services:
  - type: web
    name: ucp-poc-demo
    env: python
    buildCommand: "pip install -r requirements.txt && pip install -e ucp-python-sdk"
    startCommand: "bash start_cloud.sh"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
    plan: free
